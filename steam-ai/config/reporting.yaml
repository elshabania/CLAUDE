# Reporting conventions. Placeholders until ITC conventions are supplied
# (docs/assumptions.md). Every generated document is marked DRAFT.
rounding:
  flows: 10          # nearest 10 vehicles
  percentages: 1     # decimal places
  times_min: 0
  distances_km: 1
  speeds_kph: 0
draft_watermark: "DRAFT - generated by STEAM-AI for modeller review"
synthetic_banner: "SYNTHETIC DATA - this run was generated for testing, not a STEAM output"
diagnostic_report:
  title: "STEAM-AI Diagnostic Report"
  max_findings_in_body: 50
  include_map: true
  include_charts: true
